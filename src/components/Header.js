import Navigation from "./Navigation";

const Header = () => {
  return (
    <header className="flex flex-col gap-y-2">
      <h2>Header</h2>
      <Navigation />
    </header>
  );
};

export default Header;
